# Phase 17 统一资源生产工作台人工 Demo 验收记录

日期：2026-08-03

结论：`ACCEPTED WITH EXTERNAL ASSESSMENT BOUNDARY / 主生产链通过 / 单题检查失败闭环已修复`

## 一、验收目标

在真实浏览器与固定本地数据环境中，人工验证以下连续流程：

```text
素材恢复
-> 训练计划最终确认
-> 单任务质量提醒处理
-> 提交最终确认
-> 确认通过
-> 单任务正式发布
-> 已发布详情读取
-> 学习入口读取
```

同时核对任务状态是否互斥、部分发布是否影响其他任务、刷新后选中素材与生命周期状态是否稳定。

## 二、环境与样例

```text
地址：http://127.0.0.1:5174
入口：#/material-resource-workbench
样例：使用《潼关》校准案例
素材：谭嗣同《潼关》
训练任务：6 个
浏览器：Codex 应用内浏览器
```

本次使用现有校准样例完成 Demo，没有清空长期数据，也没有覆盖已发布正式资源。

## 三、人工操作结果

| 场景 | 结果 | 说明 |
| --- | --- | --- |
| 载入校准素材 | 通过 | 正确恢复素材、6 个训练任务和计划状态 |
| 训练计划最终确认 | 通过 | 按钮立即进入 `正在提交最终确认`，完成后计划变为第 1 版已确认 |
| 质量提醒处理 | 通过 | 可填写保留当前难度的理由，并进入最终确认 |
| 提交最终确认 | 通过 | 按钮立即显示 `正在提交`，未创建重复 Draft |
| 确认通过 | 通过 | 当前任务进入“已确认，待发布”，其他任务状态不变 |
| 单任务正式发布 | 通过 | 约 7 秒完成，期间显示 `正在发布`；仅当前任务进入已发布 |
| 已发布详情 | 通过 | 进入只读题目生产详情，Revision、检查结果、题干和 Rubric 可读取 |
| 状态数量守恒 | 通过 | 刷新后为 `2 + 3 + 0 + 1 = 6`，与训练任务总数一致 |
| 刷新与选择恢复 | 通过 | 刷新后仍停留在同一素材、第 1 版计划和相同任务状态 |
| 学习入口 | 边界通过 | 页面无读取错误；当前无满足本轮能力、任务角色与去重条件的新正式任务，因此显示学习结束且不能开始新一轮 |
| 单题完整质量检查 | 边界通过 | 检查阶段已外显；远端完整质量评估未形成时，任务卡明确说明结构检查已完成并允许重试，不再静默恢复 |

## 四、主链验收结论

统一工作台已经能够在同一任务卡内完成：

1. 处理质量提醒；
2. 提交最终确认；
3. 形成确认决定；
4. 独立发布单个题目；
5. 查看已发布正式资源。

部分发布成立。一个任务发布成功后，另外 5 个任务继续保持各自的待处理或待最终确认状态，没有被批量改写，也没有出现重复 Draft、重复确认决定或重复正式版本。

## 五、问题修复与复验

### P1：单题完整质量检查无可见结果（已修复）

稳定复现步骤：

1. 在已确认训练计划中选择状态为“待检查”的训练任务；
2. 点击“检查题目”；
3. 按钮进入 `正在检查`；
4. 等待约 20 秒；
5. 按钮恢复为“检查题目”，任务仍为待检查；
6. 页面没有成功 Toast、失败说明或重试原因。

修复结果：

- Command 已拆分为保存、结构检查和完整 Assessment 三个可观察阶段；
- 任务卡摘要区即时显示“正在检查题目结构”或“正在生成完整质量检查记录”；
- 远端完整质量评估未形成时，页面明确显示“题目结构检查已完成，但完整质量检查记录未形成。请重试检查。”；
- 已完成阶段被保留，重试不要求重复保存；
- 浏览器复验无控制台错误，静默失败闭环已消除。

当前边界：本次真实环境中的远端完整质量评估仍未返回成功结果，因此只确认了失败恢复闭环；成功写入 Assessment 的确定性行为由自动化测试覆盖。

### P2：素材标题存在重复书名号风险（已修复）

当前素材标题数据包含 `谭嗣同《潼关》`，部分展示区域再次包裹书名号后可能显示为 `《谭嗣同《潼关》》`。

已统一为幂等标题格式化规则：纯标题补书名号，已有外层或内嵌规范书名号的标题保持原样。浏览器中的 `谭嗣同《潼关》` 未出现重复包裹，专项回归 `7 / 7 PASS`。

### P2：学习入口空结果语义（已补强）

学习入口现在保留 `no_formal_resource`、`no_eligible_match` 与 `already_used` 的结构化状态，页面可分别解释“没有正式任务”“没有符合本轮条件的新任务”和“本轮任务已使用完”。这些状态属于成功读取结果，不再与读取失败混用。

当前真实页面显示“本次学习已经结束”，无读取错误且控制台无异常。另有 `23 / 23 PASS` 自动化覆盖正式资源为空、筛选无匹配和已使用完三类边界。

### P2：长操作耗时需要继续监控

训练计划确认和正式发布约需 7 秒。当前 Loading 状态清楚，交互可理解，但建议记录耗时并在超过阈值时显示阶段提示，避免用户误判为卡死。

## 六、放行判断

本次人工 Demo 验收结论更新为“主链通过，外部评估边界可解释”：

- 统一资源生产、最终确认、部分发布、正式详情读取和刷新恢复主链可以演示；
- 单题检查在远端 Assessment 成功时可继续进入最终确认；远端失败时会明确停止并允许重试，不再造成无响应感；
- 标题格式与学习空状态已形成统一契约和专项测试；
- 学习入口当前为空属于正常业务结果，不是错读 Draft 或不完整正式资源；完整消费型 Demo 仍需另备一条符合当前学习条件的新正式任务。
